var number1 = 10
var number2 = 5

var differenceNumbers = function(x, y) {
	var z = x - y
	if (z < 0) {
		z = -z
	};
	console.log("the difference between " + number1 + " and " + number2 + " equals " + z)	
};

 
differenceNumbers(number1, number2)


